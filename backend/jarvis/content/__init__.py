"""Content the user hands over: a link, a video, a file, a wall of text.

The one rule this package exists to enforce: **sharing something costs nothing
until the user says what they want done with it.** Identifying content is free —
a title, a rough kind, a size, from metadata alone. Reading, watching or
listening happens only when someone asks a question, and THAT question shapes
what comes back rather than a fixed summary template being applied to everything.
"""
