"""Content Management: finished content taken from review to publication.

See `CLAUDE.md` in this directory. `lifecycle.py` owns every change, `store.py`
every read, `kinds.py` the content types and platforms, `files.py` the media.
"""
