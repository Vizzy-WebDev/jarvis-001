"""Self-Improvement: what Jarvis has learned about its OWN work.

Distinct from Memory, which is about the user. The chain is deliberately four
steps — outcome, lesson, proposal, rule — so that noticing something once can
never become a permanent change to how Jarvis behaves.
"""

from . import apply, capture, domains, policy, reflect, store, synthesize

__all__ = ["apply", "capture", "domains", "policy", "reflect", "store", "synthesize"]
