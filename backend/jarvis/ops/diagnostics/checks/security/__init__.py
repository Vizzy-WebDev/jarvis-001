"""Security checks: detection only, by explicit scope.

None of these declares a remedy, and the registry refuses one. A build that
quietly "fixes" a security anomaly is a build that can destroy the evidence of
one before anybody has seen it — and the right response to most of these is a
decision only the owner can make.
"""
