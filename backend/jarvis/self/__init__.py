"""Self-Model: grounded, evidence-backed self-knowledge.

What Jarvis is, what it can and cannot actually do, what it is doing now and
why, and what is genuinely its own call. Explicitly not an attempt at subjective
experience — that question is out of scope.
"""

from . import model, signals, store, verify

__all__ = ["model", "signals", "store", "verify"]
